version: "3.8"

services:
  web:
    build:
      context: ./docker
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - webnet
    restart: always

networks:
  webnet:
    driver: bridge
